<html>
<head></head>

<body>
    <h2> Alarm cen walut dla usera: {{ userId }}.</h2>

    <p>

        <table class="table">
        <thead>
        <tr>
            <th>Code</th>
            <th>Name</th>
            <th>Value</th>
            <th>Set min</th>
            <th>Set max</th>
            <th>Last update</th>
            <th>Unsubscribe</th>
        </tr>
        </thead>
        <tbody>
        {% for value in valuesOutOfRange %}
            <tr>
                <td>{{ value.currency.code }}</td>
                <td>{{ value.currency.name }}</td>
                <td>{{ value.currency.value }}</td>
                <td>{{ value.min }}</td>
                <td>{{ value.max }}</td>
                <td>{{ value.currency.lastDate|date() }}</td>
                <td><a href="{{ path('unsubscribe_alert', {'id': value.id}) }}">Unsub</a></td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
        </table>

    <a href="{{ path('subscribe_alert', {'id': userId}) }}">Add new alert</a>



    </p>

</body>

</html>